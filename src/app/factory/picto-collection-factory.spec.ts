import { PictoCollectionFactory } from './picto-collection-factory';

describe('PictoCollection', () => {
  it('should create an instance', () => {
    expect(new PictoCollectionFactory()).toBeTruthy();
  });
});
